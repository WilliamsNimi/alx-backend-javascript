export default function signUpUser(firstName, lastName){
    const newPromise = new Promise((resolve, reject) => {
	});
    newPromise.then(() => {
	return ({
            firstName: firstName,
	    lastName: lastName
	});
    }).catch(() => {
	return (new Error());
    });
    return (newPromise);
}
